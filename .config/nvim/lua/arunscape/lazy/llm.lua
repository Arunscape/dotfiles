return {
  --  'huggingface/llm.nvim',
  --  opts = {
  --    -- cf Setup
  --    backend = "ollama",
  --    model = "starcoder2:7b",
  --    url = "http://localhost:11434",
  --    request_body = {
  --      -- Modelfile options for the model you use
  --      options = {
  --        temperature = 0.2,
  --        top_p = 0.95,
  --      }
  --    }
  --  }
}
